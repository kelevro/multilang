var translation = $(".translation-<%= @translation.id %>");
translation
    .find('.status-column')
    .html("<%= j status_tag(@translation.is_completed?, 'Completed', 'Incompleted') %>");

translation
    .find('.status-action')
    .html("<%= j status_control(@translation) %>");
